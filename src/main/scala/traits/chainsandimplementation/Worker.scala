package traits.chainsandimplementation

class Worker extends FooExtBar {
  /**
   * this class should override def bar() from the Bar trait
   * if this def hasn't been overriden in the FooExtBar trait
   * */
}
